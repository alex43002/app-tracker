# Copy this file to `.env` and adjust as needed.
# Base URL of the backend API (no trailing slash). The backend runs on
# port 8000 by default (uvicorn app.main:app --reload).
VITE_API_BASE_URL=http://127.0.0.1:8000
